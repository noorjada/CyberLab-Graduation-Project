# Dependencies
node_modules/
.pnp
.pnp.js

# Secrets — never commit
.env
.env.local
.env.*.local
server/.env
client/.env

# Build outputs
client/build/
mobile/build/
mobile/.dart_tool/
mobile/.flutter-plugins-dependencies

# Logs & caches
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
.DS_Store
Thumbs.db

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# Server — local DB exports & uploads (can be large / sensitive)
server/backups/
server/uploads/

# Mobile — Gradle / Flutter caches (project-local paths on D: stay outside repo)
mobile/tee-build.log
*.apk
*.aab

# OS / tooling
.claude/
